# HANDOVER.md — PP 引き継ぎ書（再開手順書）

最終更新: 2026-08-18 セッション末（く！ちゃん）— **第四弾(b) 完了**。教本の先行・後続をデータ化（D-30）＋実物確認で判読不能2箇所を確定（D-31）。`17d807b`（データ）／`d911be3`（セレクタ＋UI）の2コミットでプッシュ済み・公開反映を実測確認。次は**第四弾(a) 自作ビルダー**。
これは作業ログではなく「次のセッションの自分が迷わないための再開手順書」。セッション末に必ず更新する。

## 読み順

1. `CLAUDE.md`（ルール・コマンド・座標規約）
2. 本書（現在地・次のキュー）
3. `docs/DECISIONS.md`（なぜそうなっているか。特に D-11/D-12 の訂正履歴）
4. `docs/ROADMAP.md`（教本照合リスト・機能候補）
5. 必要なら設計書 `docs/superpowers/specs/2026-06-11-pp-dance-figures-design.md`（ステータス欄は「承認待ち」のままだが実装済み。表層と実体の差、D-09 参照）

## 現在地（2026-08-18 実測）

- バージョン: v1.3 相当。ワルツ39フィガー（教本38番のうち表のある全て）＋推奨アマルガメーション8本・アニメ再生・男性/女性/両方・日英。
- リポジトリ: https://github.com/hitomiusausa/promenade-position — **HEAD `d911be3`、origin/main と一致（実測）**。作業ツリーはクリーン（`.claude/launch.json` のみ未追跡）。
- テスト: 15ファイル **116件 緑**（両方表示の非重なり・非ブレ＋先行後続の検証23件を常設）、`tsc` OK、`vite build` OK（2026-08-18 D-31 時点）。
- 公開: https://hitomiusausa.github.io/promenade-position/ （main へのプッシュで自動デプロイ）。**公開版に第四弾(b) が載っていることをバンドル実測で確認済み**（2026-08-18: 配信中の `assets/index-31d0Uo03.js` がローカルビルドと同ハッシュで、D-31 で足した出典 `turning-lock/p.38`・条件コード `ending_backing_almost_LOD`・stub `quick-natural-spin-turn`・UI文言「先行できるフィガー」を含む。出典 `/p.NN` の出現 429）。
- ローカルの `dist/` は古い可能性があるが、コミット対象外なので放置でよい。
- 座標系: 2026-08-17 に「壁=画面下」へ改定・座標は導出（D-21）。`e1f3567` としてプッシュ・公開済み。
- 重複フォルダ `Promenade\ Position\(PP\)`（雛形のみ）は 2026-08-17 に削除済み。正本フォルダは `~/Documents/Playground/Promenade Position(PP)` の1つ。

## 直近でやったこと（2026-08-17〜18）

- フォルダ・GitHub の同期状況を実測で確認（D-17）。
- 重複フォルダの中身を全数確認して削除を判断（D-16）。
- `CLAUDE.md` / `docs/DECISIONS.md` / `docs/HANDOVER.md` を新設（D-18）。過去の判断は git 履歴と設計書から再構成し、根拠のない「なぜ」は「推定」と明記した。
- 教本『総論』（pp.2-5, 18）を受領。フロア図が鏡像（壁が左手側・右回転が左回りに見える）と判明し、利用者と実描画で確認 → 座標規約を「壁=画面下」に改め、座標をJSONから撤去して `src/geometry/derivePositions.ts` で導出する方式へ（D-21）。テスト83件緑・全30描画を目視。
- 総論の反映（D-23）: NFR系コード・`continue_rise_lower_eo_3`・`slightly_side`・辞書の教本語化・ナチュラルターン更新（ROADMAP ⚠1 クローズ）。照合シート生成を `scripts/gen-data-check.mjs`（`npm run data-check`）としてリポジトリに常設。
- 教本『ワルツ』pp.17-27 を受領（`docs/資料/pp_ワルツ.pdf`、38フィガー収録）。残り9フィガーを全歩・全列で照合し反映（D-24）。第一弾達成、`5d80c56` 公開済み。
- 第三弾: 推奨アマルガメーション8本の通し再生（D-27）。クロス描画の重なり修正（D-26）。利用者の使用検収で両方表示の男女の足の重なりを指摘 → 常設テストで構造的に禁止（D-28）→ さらにブレを指摘 → 平行移動＋連続な体の向き＋ホールド補間＋退避量の平均に作り直し、非ブレのテストも常設（D-29）。
- 第二弾: pp.23-50 の残り29フィガーを転記（D-25）。列挙の追加（SL・Hターン・フォーラウェイ等の修飾子、複合フットワーク、ダウン付きライズ、回転量の強弱、ほぼ〜）。照合シートを HTML でも生成（`docs/DATA_CHECK.html`）。表の FW 列が複合コードを生表示していたバグを修正。コード追加: 複合フットワーク3種・体重を移さないクローズ／ポジションを保つ・`in_CBMP`・ダウン／ライズ。`modifier`→`modifiers` 配列化。体重なし歩の点線描画。
- 第四弾(b): 教本 pp.16-51 の全18見開きを読み、先行・後続・註を `docs/資料/ワルツ_先行後続_転記.md`（**ローカルのみ・gitignore**）に転記 → `src/data/transitions.json`（368本・stub4・条件コード25）にデータ化（D-30）。フィガー詳細に2リスト、DATA_CHECK に「先行できる／後続できるフィガー」の表を追加。判読不能だった2箇所は利用者の実物確認で確定（D-31）。
- 未反映（第二弾以降）: 用語解説ページ（PP/CBMP/OP/PO/SL/CBM・足の位置図・フットワーク図・LOD図）、`B`（ボール）等のフットワーク、他種目のタイミング表（総論p.5）。
  ※ 「教本の先行・後続・註のデータ化」は第四弾(b)で完了（D-30/31）。
  ※ 「教本には残り28フィガー」は**古い記述だったので訂正**: 教本38番は第二弾（D-25）で全収録済み（表のある39エントリ。#7・#13 が2フィガーずつ、#36 は表なし）。実体が無いのは**表の無い stub 4件**（ウイング／クロス・ヘジテーション／ウイーブ／クイック・ナチュラル・スピン・ターン）だけで、いずれも教本『ワルツ』に単体の表が無いため別資料が要る（D-30）。

## 第三弾（2026-08-17 利用者と合意）: アマルガメーション再生

達成: 教本 p.51 の推奨8本を `amalgamations.json` にデータ化し、`#/amalgamation/waltz/<id>` で通し再生（D-27、設計追補 `docs/superpowers/specs/2026-08-17-amalgamation.md`）。残り＝利用者の使用検収（推奨1を再生して流れが追えるか）→ コミット・プッシュ。次段階候補: 自作ビルダー、教本の先行・後続のデータ化。

## 第二弾（2026-08-17 利用者と合意）: ワルツの残りフィガーを全部入れる

進捗: 29フィガーを転記して計39（D-25）。残り＝利用者の独立検収（DATA_CHECK.html を教本と見比べる）→ 相違の修正 → コミット・プッシュ。「両方」表示の表は現状維持（男性の表）と合意。

## 第一弾の完成条件（2026-08-17 利用者と合意・達成）

**第一弾の完成 = (a) ワルツ10フィガーのデータが教本照合をすべてクローズし、公開版に反映されていること。**
**達成（2026-08-17）**: 照合10/10（D-23/D-24、DATA_CHECK 全 ✅）、`5d80c56` としてプッシュ・公開（公開版データに新コードが載っていることを curl で確認）。残るのは利用者の公開版目視と教本との独立検収のみ（次のキュー0-1）。
クローズの判定: ROADMAP「最優先」の9項目それぞれについて、①利用者が教本と見比べて「一致」または「教本値へ修正済み」と宣言 ②修正分は `npm test` 緑でコミット・プッシュ済み ③DECISIONS.md に「何を・何を根拠に」が1行ある。9項目以外でも照合中に見つかった相違は同じ扱いで追加する。
機能追加（クイズ等）は第一弾に含めない（第二弾以降で再合意）。

## 次のキュー（上から順に）

0. **第四弾(a) 自作アマルガメーション・ビルダー（ここから）** — 第四弾(b) が済んだので「次に来られるフィガー」を教本根拠で出せる。
   - API: `src/data/transitions.ts` の `followingFigureIds(dance, figureId, realIds)` が実体フィガーの id だけ返す（stub 4件と群参照 `to: null` の2件を落とす）。条件バッジや歩範囲つきで見せたいなら `followingOf()` を使い `conditions` / `toSteps` / `viaFigure` を読む。
   - 合成は既存の `src/data/amalgamation.ts` の `buildAmalgamation` がそのまま使える（`{figure, steps?, note?}` の配列を渡すだけ）。表示も `FigureDetailView` を再利用できる（推奨8本と同じ作り。D-27）。
   - 保存は localStorage、キーは `pp-` 接頭辞（既存の `pp-locale` と同じ規約）。
   - 設計 → 合意 → 実装の順で。まず Plan モードで画面設計を出して利用者の合意を取る。

0b. ~~**第四弾(b) の独立検収**~~ → **済み（2026-08-18 利用者がコミット前に `docs/DATA_CHECK.html` と教本 pp.17-50 を突き合わせ、一致を宣言）**。判読不能だった2箇所も同日に実物確認で解決済み（D-31）。第四弾(b) は検収まで完了。

1. **D-25 の独立検収（後回しと合意）** — `docs/DATA_CHECK.html` を教本 pp.23-50 と見比べる（#20 は確認済み。ランニング・スピン・ターンのスウェイ列など）。相違は「フィガー／歩／列／教本の値」で。
2. 照合で座標（見た目）の不自然さが出たら、JSONでなく `derivePositions.ts` のパラメータで直し、ISTD値の修正とは別コミットに分ける（スコープ混在の禁止）。
3. 残りの候補 — (c) 短期機能（クイズ・習得チェック・ズーム・カウント表示） (d) 用語解説ページ（総論の内容） (e) フィガー一覧のグルーピング／フィルタ (f) クイック・ナチュラル・スピン・ターン（教本に表なし。別資料があれば。今は stub として transitions から参照されている） (g) 残る stub 3件（ウイング／クロス・ヘジテーション／ウイーブ）の実体化——教本に単体の表が無いので別資料が要る。

## 確立した規律（このプロジェクト固有）

- データ修正は利用者の教本照合の指摘だけを根拠にする。推測で直さない（D-15）。
- コード値の追加は `types.ts` の列挙＋ja/en 両辞書をセットで（D-04）。
- コミット前 `npm test` 緑。`git add -A` 禁止（CLAUDE.md ルール7）。
- 判断は DECISIONS.md に3点セットで追記。訂正は消さず残す。

## 気づき・未処理（スコープ外なので直していない）

- 設計書のステータス欄は「実装済み（v1.1）＋D-21追補」に更新済み（2026-08-17）。
- 計画書 `2026-06-11-pp-v1.md` のチェックボックスが全て未チェックのまま。実装完了は git で証明済みなので、更新するか「完了・履歴文書」と冒頭に書くかは利用者判断。
- `.superpowers/brainstorm/` にブレスト時の一時ファイルが残っている（git 管理外か要確認）。消すかどうかは利用者判断。
- `.claude/launch.json`（ブラウザ確認で `npm run dev` を起動するための設定）が未追跡のまま。要るならコミット、要らなければ消す（利用者判断）。
- ルート直下に紛れ込んでいた `2026-08-17-preceding-following.md` と `cowork-kickoff.md` は 2026-08-18 に削除（スペックの正本は `docs/superpowers/specs/2026-08-17-preceding-following.md`。本文が同一なことを diff で確認してから消した）。
- 第四弾(b) の転記メモ `docs/資料/ワルツ_先行後続_転記.md` は **gitignore 済み＝ローカルのみ**（D-22）。このフォルダを別マシンに持っていくと消える。リポジトリ側の正本は `src/data/transitions.json` と `docs/DATA_CHECK.*`。
- ROADMAP 短期2「習得チェック」は localStorage 前提。将来サイト統合時にストレージキーの衝突を避けるため `pp-` 接頭辞（既存の `pp-locale` と同じ規約）で統一すること。

## 環境メモ

- 開発: `npm run dev` ／ 検証: `npm test` ／ データだけ: `npm run validate-data`
- Node 20（deploy.yml と同じ）
- Cowork から作業する場合: フォルダを接続 → 変更ファイルは `device_commit_files` で書き戻す → コミット・プッシュは利用者がターミナルで（Cowork経由の git は `.git/index.lock` を消せずコミットが失敗する。`git add` は通るがロックが残るので `rm -f .git/index.lock` が必要）
