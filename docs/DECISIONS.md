# DECISIONS.md — PP 判断記録

書式: **判断内容 / なぜ / 弱いモデルが間違えそうな点**。日付と根拠（コミット・文書）を併記。誤った判断も消さず「訂正」として残す（fable5-agent-playbook 第7章）。
2026-06-11〜12 の項目は 2026-08-17 に git 履歴・設計書・ROADMAP から遡って再構成したもの（当時の会話は参照できていない。「なぜ」は設計書に明記があるものだけ断定、それ以外は「推定」と付記）。

---

## 2026-06-11 設計（根拠: 設計書 a0f90f7 / 430eca8）

### D-01 静的SPA（Vite + React + TS）＋静的JSON。サーバー・DBなし
- なぜ: 配布先が「本人のダンスサイトのサブディレクトリ」で、`dist/` をコピーするだけで動く必要がある（設計書 §2）。
- 間違えそうな点: 「フィガーが増えたらDBが要る」と判断してバックエンドを足すこと。データはファイル追加で増やす設計。

### D-02 ハッシュルーティング（`#/figure/waltz/natural-turn`）＋ `base: './'`
- なぜ: サーバー側のリライト設定なしで、どのサブディレクトリでも動くため（設計書 §2）。
- 間違えそうな点: 「URLがきれいでない」を理由に history ルーティングへ移すと、GitHub Pages・サブディレクトリ配置で 404 になる。

### D-03 足型図・アニメともに SVG
- なぜ: 拡大しても崩れず、足1つを部品化して重心色を前部/後部で塗り分けやすい（設計書 §2, §3）。
- 間違えそうな点: Canvas に移すと歩のクリック選択（SVG要素ごとの onClick）とテスト（DOM で検証）を作り直すことになる。

### D-04 ISTD項目はコード値で保持し、表示時に辞書で変換
- なぜ: アライメント・フットワーク等は決まった語彙の組み合わせなので、データ1セットで10言語に対応できる（設計書 §2「多言語化の仕組み」）。
- 間違えそうな点: データJSONに日本語文字列を直書きすること。`src/types.ts` の列挙にない値は検証テストで落ちる。

### D-05 i18n ライブラリを入れず自前実装（辞書＋Reactコンテキスト）
- なぜ: 必要なのは「キー→文言」「コード値→文言」の変換だけで、複数形処理などは不要（設計書 §5）。
- 間違えそうな点: 「標準的だから」と i18next 等を導入して依存を増やす。

### D-06 アプリ内エディタは作らない。データ修正は会話ベース
- なぜ: 足型データはClaudeが全量生成し、利用者が教本と照合して相違を会話で指摘する運用（設計書 §1）。
- 間違えそうな点: 「便利だから」とエディタを実装し始める。v1 の完成条件に含まれない。

### D-07 図示するのは空間情報（位置・角度・重心・体の向き）だけ。カウント・回転量・アライメント・ライズ&フォール・スウェイ・CBM はテキスト
- なぜ: 図に詰め込むと読めなくなる。スウェイの図示は将来拡張（設計書 §3）。
- 間違えそうな点: 「見た目が寂しい」を理由に図へ情報を足すこと。ROADMAP 中期8「スウェイ・ライズの視覚化」として別途判断する。

### D-08 v1 スコープ＝ワルツ基本フィガー10個・3画面・日英2言語
- なぜ: 英語はISTD原語なので翻訳コストが低く、i18nの仕組み検証に最適（設計書 §8）。残り8言語は辞書ファイル追加のみで対応できる状態にしておく。
- 間違えそうな点: 「10言語対応」の文言を見て、10言語分の辞書がある前提で作業する。実体は `DICTIONARIES = { ja, en }` の2言語（`src/i18n/index.tsx`）。

## 2026-06-11〜12 実装期（根拠: git log）

### D-09 TDD・タスク単位コミット（計画書 eacf8d6、13タスク）
- なぜ: 計画書に「失敗するテスト→実装→緑→Commit」の手順で書かれている。
- 間違えそうな点: 計画書のチェックボックスが全て `- [ ]` のまま（未チェック）だが、実装は完了している（git log と `npm test` 71件緑で確認、2026-08-17）。**チェック状態は実体ではない。**

### D-10 座標系: LOD=+x（画面右）、壁=画面上、angle 0=画面上向き・時計回り正
- なぜ: 教本の図と対応づけやすい（計画書「前提知識」）。
- 間違えそうな点: 数学座標（y上向き・反時計回り）で角度を計算する。SVGは y 下向き。

### D-11 女性パレット: 「淡色」→「同じ濃さの対比色」に **訂正**（1b4622b → aeb43a0 → 359509f）
- 経緯: 両方表示の男女区別として最初は女性を淡色化した（1b4622b）。同日に「同じ濃さの対比色（マゼンタ/バイオレット/オリーブ）」へ変更し（aeb43a0）、単体表示・アニメ・選択リング・表のFW列まで一貫させた（359509f）。
- なぜ（推定・コミット文から）: 淡色だと女性パートが「薄い＝副次的」に見え、重心色の判別も落ちる。役割＝色のアイデンティティとして扱う方が一貫する。
- 間違えそうな点: 男女区別のために再び透明度で差をつけること。区別は色相と歩番号バッジ（男性=塗りつぶし/女性=白抜き）で行う。

### D-12 再生終了後の挙動: 「俯瞰図へ復帰」→「最終地点で停止・⏮で復帰」に **訂正**（d6ca8e1 → f0f59ea）＋ 歩選択でアニメ保持を解除（01218a5）
- 経緯: 最初は再生終了で自動的に俯瞰図へ戻した。後に最終フレームで止めて確認できるようにし、⏮ボタンで俯瞰図に戻す形へ変更。さらに終了後に歩を選択すると保持を解除してフォーカス表示に戻す不具合修正。
- なぜ（推定）: 最後の足位置を確認したいのに一瞬で消える。
- 間違えそうな点: `FigureDetail.tsx` の `animActive = anim.playing || anim.t > 0` を「終了したら false」に単純化すると D-12 を巻き戻す。

### D-13 歩選択でフォーカス表示（該当歩フル色・他は薄表示）＋体の向き矢印（de8b570）
- なぜ: 図が混み合うフィガーで見たい歩だけ浮かせる（ROADMAP 短期3「ズーム」の前段）。
- 間違えそうな点: 薄表示を「非表示」にすると位置関係が分からなくなる。

### D-14 ビルドはテスト＋型チェックをゲートにする（`vitest run && tsc && vite build`）／デプロイは GitHub Actions（dab1c5f）
- なぜ: データ検証テストが赤のまま公開されるのを構造で防ぐ。
- 間違えそうな点: ローカルの `dist/` を手でアップロードする運用に戻す。`dist/` はコミット対象外。

### D-15 データは「Claude生成の参考値」と README/ROADMAP に明記し、自信の低い9箇所を ROADMAP に列挙（fd64dc2）
- なぜ: 事実（教本）と文書（生成データ）の境界を明示するため。推測で直さず利用者の照合を待つ。
- 間違えそうな点: 「ISTDの一般知識ではこうだ」と教本を見ずにデータを修正すること。教本の版・流派で差がある項目がある。

## 2026-08-17 運用整理

### D-16 バックスラッシュ入りフォルダ `Promenade\ Position\(PP\)` を削除（利用者が実施）
- なぜ: 中身は 2026-06-11 19:25 の Vite 雛形10ファイルのみで、本体にしかない情報はあっても雛形側にしかない情報はゼロ（device_list_dir で全数確認）。git 管理外。
- 間違えそうな点: フォルダ名が似ているので「2つの正本がある」と誤認して統合作業を始めること。統合すべき差分は存在しなかった。

### D-17 ローカルと GitHub の同期を実測で確認
- 実測: ローカル `git status` = `main...origin/main` 変更なし・ahead/behind 0。GitHub HEAD = ローカル HEAD = `fd64dc2`。クローンして `npm test` → 71件緑・`tsc` OK。
- 間違えそうな点: 「GitHubに上げた記憶」を根拠に同期済みと言う。ハッシュ一致で裁定する。

### D-18 CLAUDE.md をルート、DECISIONS.md / HANDOVER.md を `docs/` に置く
- なぜ: CLAUDE.md は Claude Code が自動で読む慣例位置。判断記録・引き継ぎ書は既存の `docs/ROADMAP.md` と並べて1箇所に集める。
- 間違えそうな点: 同じ内容を README にも書いて正本を割る。README は「開発・公開・データ運用の入口」だけにとどめ、詳細は docs/ へ参照で飛ばす。

### D-19 「完成」第一弾 = (a) 教本照合の全項目クローズ（利用者決定）
- なぜ: データが正しくないまま機能を足すと、後で全機能を再検収することになる。土台確定→一発実装の順（プレイブック第8章 AX）。
- 間違えそうな点: 照合作業の途中で「ついでに」クイズ等の機能に手を出してスコープを混ぜること。相違の指摘は利用者の教本が唯一の根拠で、Claude の ISTD 知識で先回りしない。

### D-20 照合シート `docs/DATA_CHECK.md` を JSON から機械生成（手書きしない）
- なぜ: 手で転記すると転記ミスが「教本との相違」に見えて偽欠陥を生む。生成物なので、データを直したら再生成して常に実体と一致させる。
- 間違えそうな点: DATA_CHECK.md を直接編集して JSON と食い違わせること。正本は `public/data/waltz/*.json`。

## 2026-08-17 座標系の修正

### D-21 フロア図の鏡像を修正: 「壁=画面上」→「壁=画面下」＋ 座標をJSONから撤去して導出に変更（**D-10 の訂正**）
- 経緯: 教本『総論』p.4（LODは反時計回り。LODに面して右手側が壁）と突き合わせ、旧規約（LOD=右・壁=上）は鏡像の部屋だと判明。実描画でナチュラルターン男性1-3歩の足の向きが 45°→315°→270°（画面上で反時計回り＝左回転に見える）ことを利用者と両方で目視確認。さらに旧データは足の左右の並びは正しく回転方向だけ鏡像という混在状態で、単純な上下反転では直らない（反転すると左右の並びが壊れる）。
- 判断: (1) 規約を「壁=画面下」に改め、方向→角度を 中央0/DC45/LOD90/DW135/壁180/逆壁斜め225/逆LOD270/逆中央斜め315 で固定。(2) `position` / `startPositions` を全10フィガーのJSONから削除し、`src/geometry/derivePositions.ts` がアライメント・足・足の位置（前進/横/クローズ/クロス/PP等）から計算する。JSONに座標があれば検証で弾く。(3) 女性は男性の開始向きとの関係（180°差=クローズド、それ以外=PP）から開始位置を置く。
- なぜ導出にしたか: 保存された座標と ISTD 値の二重管理は正本割れの典型（プレイブック第3章 BC「状態は保存せず導出する」）。今回のように規約が変わったとき、120歩ぶんの手直しを一切せずに全図が追従した。以後フィガー追加はISTD値だけ書けばよい。
- 検収: `npm test` 83件緑（新規: 座標規約テスト9件・検証テスト3件）。全10フィガー×男性/女性/両方の実描画30枚を撮影し、右回転が右回りに見えること・LODに面した人の右手が画面下であることを目視。
- 間違えそうな点: ①「見た目を少し直したい」からとJSONに座標を書き戻すこと（検証で落ちる。パラメータを直す）。②旧規約の角度（DW=45）で新しいフィガーを考えること。③導出はISTDの「足の位置」を機械的に置いた近似で、教本の足型図と完全一致はしない——ISTD値が正なら図もそれに従っているだけなので、図の違和感はまずISTD値の照合で裁定する。④PP中の横歩（シャッセ フロム PP 2-4歩）は体の向き基準で置くため、実際より中央寄りに進んで見える。必要なら `side_in_PP` の使用または導出側の扱いを再検討する（未対応と記録）。

### D-22 教本PDF（`docs/資料/`）はローカルのみ・git 管理外。ISTD ベーシックフィガーの内容（足の位置・アライメント等）はアプリのデータとして利用してよい
- なぜ: 教本のスキャンは著作物なので公開リポジトリに載せない。一方、ベーシックフィガーの技術内容は一般的な概念であり、利用者がダンスの先生に確認済み（2026-08-17）。
- 間違えそうな点: 「資料を参照した」記録を残そうとしてPDFをコミットすること。参照は DECISIONS/DATA_CHECK にページ番号で書く（例: 総論 p.4、Natural Turn p.18）。逆に、教本の表の文言をそのまま UI に貼るのではなく、コード値＋辞書という既存の構造に翻訳して取り込む。

### D-23 総論（p.2-4）と Natural Turn（p.18）の反映: NFR系コード・`slightly_side`・辞書の教本語化・NTデータ更新
- 判断: (1) `RISE_FALLS` に `commence_rise_eo_1_nfr` / `commence_rise_eo_2_nfr`（NFR=ノーフットライズ付きのライズ開始）と `continue_rise_lower_eo_3`（教本の「ライズ継続、3の終わりにロア」）を追加。NFRは独立フラグでなく複合コード（案A）——教本での出方が「ライズ開始 NFR」しか確認できていないため。他の組み合わせが出たらフラグ方式（案B）へ再検討。(2) `MODIFIERS` に `slightly_side`（教本の足の位置(2)「前、少し横に」(8)「後ろ、少し横に」）。(3) 日本語辞書を教本語へ: 「逆LODの壁斜め」→「逆壁斜め」、「逆LODの中央斜め」→「逆中央斜め」、「〜にポイントして」→「〜に向けて」。コード値・英語は不変。(4) natural-turn.json: 男性4歩目・女性1歩目を NFR 付きに、3・6歩目を `continue_rise_lower_eo_3` に、回転量欄の「1で右回転を始め」「4 右回転を継続」と女性2歩目「体の回転は少なく」をメモとして追加。教本p.18のそれ以外の値は現データと一致（=ROADMAP ⚠1 クローズ）。
- なぜ: 利用者の教本が正（D-15）。表現できない値（NFR・少し横に）は列挙に足し、文言だけの差は辞書で吸収する（コード値を増やさない）。
- 検収: `npm test` 84件緑（辞書テスト1件追加）、`tsc` OK。照合シートは `npm run data-check` で再生成し、NT に ✅ を付けた。
- 間違えそうな点: ①「ダウン、4の終わりにライズ」の「ダウン」を新コードにしないこと——ロア後の状態説明で、既存の `commence_rise_eo_1(_nfr)` に含意される。②`continue_rise_lower_eo_3` と `up_lower_eo_3` はどちらも「3で下りる」だが教本の文言が違うだけの可能性がある。他フィガーで教本が「アップ、3の終わりにロア」と書くなら残す、書かないなら統合を検討（両方正しく前提が違うだけ、を先に試す）。③照合シートは生成物。手で直さない。

### D-24 教本『ワルツ』pp.17-27 と全10フィガーを照合し反映（第一弾の照合クローズ）
- 判断: 教本の表を1歩ずつ現データと突き合わせ、相違をデータに反映。表現できない値は列挙に追加した。
  - 追加コード: フットワーク `H_IE_WF`（H・足のIE・WF）`T_IE`（TのIE）`H_then_both_T`（H、次いで両足のT）／足の位置 `close_no_weight`（体重を移さないでクローズ）`hold_position`（ポジションを保つ）／修飾子 `in_CBMP`／ライズ `down`（ダウン）`rise`（ライズ）。
  - 構造変更: `stepDescription.modifier`（単数）→ `modifiers`（配列）。教本は「OPでCBMPに」「PPでCBMPにアクロスして」と修飾を自由に重ねるため。旧キー `modifier` は検証で弾く。
  - 描画: 体重を移さない歩（`close_no_weight` / `brush`）はフットワーク色を保ったまま点線（`Foot.noWeight`、アニメでも `landedNoWeight` で維持）。
  - データの主な訂正（自信が低かった9項目の結果は ROADMAP に転記）: リバースコルテ男性2-3歩のモデル（「体重を戻す」→「体重を移さないでクローズ」＋「ポジションを保つ」）、ヘジテーション男性5歩目のFW（フラット→H・足のIE・WF）と6歩目（体重なし→TのIE＋体重を移さないクローズ）、スピンターン4歩目の回転量（1/2は4歩目・5歩目は継続）、ウィスク女性1歩目 CBM なし・2歩目「斜めに後退」、シャッセ男性1歩目 CBM なし、各フィガーの NFR、3・6歩目の「ライズ継続、3の終わりにロア」、回転量欄の文言（「1で右回転を始め」等）をメモへ。
  - 「アップ、3の終わりにロア」と「ライズ継続、3の終わりにロア」は教本内でも使い分けられている（ウィスク・シャッセ・コルテ3・OC・スピン6＝アップ／クローズドチェンジ・NT・RT・スピン3・ヘジ3・コルテ女性3＝ライズ継続）ため両コードを維持（D-23 ②の裁定）。
- 検収: `npm test` 87件緑（色・書式・体重なしのテスト追加）、`tsc` OK、全10フィガー×3表示の再描画を目視（体重なし歩が点線、コルテ3歩目が動かない等）。`npm run data-check` で照合シート全フィガー ✅。
- 間違えそうな点: ①`hold_position` は足を動かさない歩。「体重を戻す（replace_weight）」と混同して足を戻す座標を書かない。②`H_IE_WF` 等の複合フットワークは文字分割で表示しない（`formatFootwork` は `^[HT]+$` のときだけ分割）。③教本の「CBM 1で少し」は `cbm: true`＋メモ「1で少しCBM」で表す（cbm に段階は持たせない）。④教本の後続・先行・註（PPで終わる場合の足の位置など）はデータ化していない——次のスコープ。
